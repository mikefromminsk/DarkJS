package org.pdk.net.ws;

class WsClientServerTest {
/*
    @Test
    void send() throws InterruptedException {
        WsClientServer proxy = new WsClientServer(9001);
        proxy.start();
        WsClientServer from = new WsClientServer(9002);
        from.start();
        WsClientServer dest = new WsClientServer(9003);
        dest.start();
        from.send(dest.getNodeName(), "ConsoleModule/log", new Node[]{new NodeBuilder().createString("testMessage")});
        from.thread.join();
    }*/
}